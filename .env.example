# ==================================================
# CONFIGURAÇÕES DE AMBIENTE - SISTEMA GESTÃO FINANCEIRA
# ==================================================
# ⚠️  IMPORTANTE:
# 1. Copie este arquivo para .env e configure os valores
# 2. NUNCA commite o arquivo .env no Git!
# 3. Use valores fortes em produção
# ==================================================

# ==================================================
# SEGURANÇA (CRÍTICO!)
# ==================================================
# NUNCA use valores padrão em produção!
# Gere uma chave secreta forte com:
# python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=SUBSTITUA_POR_UMA_CHAVE_FORTE_DE_64_CARACTERES_HEXADECIMAIS

# ==================================================
# BANCO DE DADOS
# ==================================================
# Formato: postgresql://usuario:senha@host:porta/nome_banco

# Desenvolvimento (local)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/gestao_financeira

# Produção (VPS/Cloud) - USE SENHA FORTE!
DATABASE_URL=postgresql://usuario:SENHA_FORTE_AQUI@host:5432/gestao_financeira

# ==================================================
# AMBIENTE
# ==================================================
# Valores aceitos: development, production
# EM PRODUÇÃO, USE: production
FLASK_ENV=production
FLASK_APP=run.py

# ==================================================
# REDIS (OPCIONAL - recomendado para produção)
# ==================================================
# Usado para rate limiting e cache
# Se não configurado, usará memória (não recomendado para múltiplos workers)
# REDIS_URL=redis://localhost:6379/0

# ==================================================
# CONFIGURAÇÕES OPCIONAIS
# ==================================================
# Porta do servidor (padrão: 5000)
# PORT=5000

# Número de workers do Gunicorn (produção)
# Recomendado: (2 x número_de_cores) + 1
# WORKERS=4

# Timeout do Gunicorn em segundos
# TIMEOUT=120

# ==================================================
# INSTRUÇÕES DE SEGURANÇA
# ==================================================
# 1. SECRET_KEY: Gere uma chave única e forte
# 2. DATABASE_URL: Use senha forte e complexa
# 3. FLASK_ENV: Sempre "production" em produção
# 4. Mantenha .env fora do controle de versão
# 5. Configure permissões adequadas: chmod 600 .env
# ==================================================
